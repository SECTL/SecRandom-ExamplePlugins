# SecRandom Example Plugin

Plugins receive metadata through `IPluginBuildContext.PluginInfo` and `IPluginRuntimeContext.PluginInfo`.

Runtime data is stored under:

`data/configs/plugins/com.example.plugin`
